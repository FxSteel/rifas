---
name: DB changes manual only
description: User wants to make all database changes manually themselves - never execute SQL via Supabase MCP tools
type: feedback
---

Never execute SQL or database changes automatically. If DB changes are needed, provide the SQL to the user so they can run it manually.
